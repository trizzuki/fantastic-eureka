name: Test

on:
  workflow_dispatch:

jobs:
 test:
    runs-on: windows-2022

    steps:
      - name: Hello
        run: echo "Hello World"

      - name: PowerShell
        shell: pwsh
        run: |
          Write-Host "Runner OK"
          Get-Date
